{
  "name": "@huelegood/admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "sh -c 'next start -p ${PORT:-3005}'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@huelegood/shared": "^0.1.0",
    "@huelegood/ui": "^0.1.0"
  }
}
